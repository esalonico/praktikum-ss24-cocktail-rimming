#include <Arduino.h>

// global variables (macros)
#define LED 34

// runs only once at setup
void setup()
{
  Serial.begin(115200);
  Serial.println("Setup done!");

  // setup led
  // pinMode(LED, OUTPUT);
}

// runs main code (while True)
void loop()
{
  // digitalWrite(LED, 1);
  // Serial.println("Led ON");
  // delay(1000);
  // digitalWrite(LED, 0);
  // Serial.println("Led OFF");
  // delay(1000);
}